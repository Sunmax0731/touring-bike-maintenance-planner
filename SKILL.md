# SKILL

## Start Order

1. `README.md` で閉域アルファ範囲を確認する。
2. `docs/source-idea-pack.json` で NON PICKUP 行、metadata、created_idea pack の根拠を確認する。
3. `samples/representative-suite.json` と `src/core/productProfile.js` を確認する。
4. `cmd.exe /d /s /c npm test` を実行する。
5. Release 作業では `docs/releases/v0.1.0-alpha.1.md` を本文に使う。

## Validation

- 自動検証は代表シナリオ、required docs、QCDS metrics、source pack、文字化け断片、release evidence を確認する。
- 手動検証はユーザーが実施するため、Codex 側では未実施として記録する。
